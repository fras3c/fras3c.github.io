{
  "name": "fras3c-site",
  "version": "1.0.0",
  "description": "My personal website",
  "scripts": {
    "build": "node build-content.js",
    "import:scopus": "node scripts/import-scopus.js --file scopus.csv",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "csv-parse": "^5.5.5",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0"
  }
}
